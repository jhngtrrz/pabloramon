---
const PROJECTS = [
  {
    title: "Renderers e Isométrías",
    description:
      "Animación 3D, render y video. Lleva tus ideas a otro nivel con visualizaciones impactantes: imágenes, videos y animaciones 3D que dan vida a tus proyectos.",
    images: [
      "/projects/render/render0.jpg",
      "/projects/render/render1.jpg",
      "/projects/render/render2.jpg",
      "/projects/render/render3.jpg",
      "/projects/render/render4.jpg",
      "/projects/render/render5.jpg",
      "/projects/render/render6.jpg",
      "/projects/render/render7.jpg",
      "/projects/render/render8.jpg",
      "/projects/render/render9.jpg",
    ],
  },
  {
    title: "Planimetría en general",
    description:
      "Digitalización de planos de arquitectura, estructuras e instalaciones en 2D y 3D con Autocad, Revit y Archicad. Diseños completos para viviendas y todo tipo de inmuebles: desde obras nuevas y proyectos menores, hasta trámites municipales, regularizaciones y gestiones con entes públicos. Precisión y calidad en cada lámina.",
        images: [
      "/projects/planimetria/planimetria0.jpg",
      "/projects/planimetria/planimetria1.jpg",
      "/projects/planimetria/planimetria2.jpg",
    ],
  },
];
---

<div class="flex flex-col gap-y-16">
  {
    PROJECTS.map(({ images, title, description }) => (
      <article class="flex flex-col space-x-0 space-y-8 group md:flex-row md:space-x-8 md:space-y-0">
        <div class="w-full md:w-1/2">
          <div class="relative flex flex-col items-center col-span-6 row-span-5 gap-8 transition duration-500 ease-in-out transform shadow-xl overflow-clip rounded-xl sm:rounded-xl md:group-hover:-translate-y-1 md:group-hover:shadow-2xl lg:border lg:border-gray-800 lg:hover:border-gray-700 lg:hover:bg-gray-800/50">
            <div class="relative w-full aspect-[4/3]">
              {images.map((src, index) => (
                <img
                  src={src}
                  alt={title}
                  class={`absolute inset-0 w-full h-full object-cover object-center transition-opacity duration-500 ease-in-out ${index === 0 ? "opacity-100" : "opacity-0"}`}
                  loading={index === 0 ? "eager" : "lazy"}
                  data-slide-id={title}
                  data-slide-index={index}
                />
              ))}
            </div>
            <script define:vars={{ imageCount: images.length, title }}>
              (function() {
                const slides = document.querySelectorAll(`[data-slide-id="${title}"]`);
                if (!slides.length) return;
                
                let current = 0;
                const total = imageCount;

                function changeImage() {
                  slides[current].classList.remove('opacity-100');
                  slides[current].classList.add('opacity-0');
                  
                  current = (current + 1) % total;
                  
                  slides[current].classList.remove('opacity-0');
                  slides[current].classList.add('opacity-100');
                }

                setInterval(changeImage, 4000);
              })();
            </script>
          </div>
        </div>

        <div class="w-full md:w-1/2 md:max-w-lg">
          <h3 class="text-2xl font-bold text-gray-800 dark:text-gray-100">
            {title}
          </h3>
          <div class="flex flex-wrap mt-2">
            <ul class="flex flex-row mb-2 gap-x-2" />

            <div class="mt-2 text-gray-700 dark:text-gray-400">
              {description}
            </div>
            <footer class="flex items-end justify-start mt-4 gap-x-4" />
          </div>
        </div>
      </article>
    ))
  }
</div>
